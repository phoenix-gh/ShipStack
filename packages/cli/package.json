{
  "name": "@shipstack-dev/cli",
  "version": "0.1.0-alpha.0",
  "description": "CLI for creating and extending ShipStack apps.",
  "license": "MIT",
  "private": false,
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/phoenix-gh/ShipStack"
  },
  "files": [
    "dist",
    "templates",
    "package.json"
  ],
  "bin": {
    "shipstack": "./dist/cli.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/make-bin-executable.mjs",
    "prepack": "pnpm build && node scripts/copy-templates.mjs",
    "postpack": "node scripts/clean-templates.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm build && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@shipstack-dev/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.5.4",
    "typescript": "^6.0.3"
  }
}
